#include "SyntaxTree.h"

namespace geneprog {

template <typename T>
SyntaxTree::SyntaxTree(Node* root) {
    root = root;
    currentNode = root;
}

template <typename T>
void SyntaxTree::insertNode(T value, int key) {
    
}
